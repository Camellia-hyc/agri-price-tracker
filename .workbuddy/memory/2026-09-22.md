# 2026-09-22

## 农价通（agri-price-tracker）项目通读与体检

蒿弈丞让"读这个文件夹"，实际是对整个工作区做了一次通读。产出是聊天里的项目现状说明，
没有生成新文件（他没要报告）。

### 实测数据现状（和文档里写的不一样）

- 产品 20（5 类 × 4）；市场 **9** 个（真实官方市场全称）；记录 **16200** 条
- prices.json 1.28MB；日期 2026-06-25 → 2026-09-22，90 天
- 真实数据只有 **1 天**（2026-09-22，78 条 = 19 品种 × 9 市场，小麦当天无报价）
  → 占 0.48%，其余 89 天全是 mock。`data/history/` 只有一个快照文件。
- 市场 id 已被爬虫改成哈希式：`bj_564be9aa` / `xa_9baa00d4` 等，不再是 `bj_xinfadi`

### 文档与代码的偏差（要改的话改这里）

代码是对的：`PriceCompare.tsx` / `TrendAnalysis.tsx` 都写了"市场 id 动态、失效则回退"，
`DEFAULT_MARKET_COUNT = 5` 取 markets.json 前 5 个。

仍然停在 9-17 旧口径的文档（写着 10 市场 / 18000 条 / 1.4MB）：

- `docs/README.md` 第 13、89、90 行
- `docs/架构设计.md` 第 13、14、62、70、81、97 行
- `docs/功能说明.md` 第 52、83 行（默认市场名写的是"北京新发地、上海江桥、广州江南、
  成都农产品中心、西安欣桥"，实际取前 5 个 = 北京/上海/广州/杭州/郑州）
- `docs/核心代码讲解.md` 第 52、149 行
- `docs/实验报告素材.md` 第 39、62、88 行

新的正确口径只写在 `docs/数据说明.md`（今天 15:02）。

### 构建与版本控制

- `dist/` 是 15:18 构建的，`dist/mock/prices.json` 与 `public/mock/` 的 md5 一致 → 产物是新的
- chunk 拆分符合架构文档：index 41KB / echarts 500KB / react 180KB / 三个页面各 7-8KB
- git 只有 2 个 commit（v1.0 + _redirects）。9-22 的爬虫、数据、docs、storage、
  build-report.mjs 全部未提交；`.gitignore` 里没有 `storage/`（Crawlee 运行时目录，108K）
  和 `data/history/` 的条目，容易被误提交

### 待他决定

- 要不要把 6 份文档统一到"数据说明.md"的新口径
- 要不要补 .gitignore（storage/、data/history/ 是留还是 ignore）
- 趋势页现在展示的"趋势"本质是伪随机数模型，要不要真攒 90 天
